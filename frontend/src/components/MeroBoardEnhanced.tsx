// This is a comprehensive enhanced version of MeroBoard with all TeamSync features
// We'll migrate the existing MeroBoard.tsx to use this enhanced version
// For now, keeping the original and adding features incrementally

export { default } from './MeroBoard';

